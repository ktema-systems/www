// @ts-check
import { defineConfig } from 'astro/config';

// `site` stays unset until the production domain is chosen. Setting it is what
// lets Astro emit absolute canonical URLs and a sitemap.
export default defineConfig({});
